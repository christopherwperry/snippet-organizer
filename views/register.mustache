<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Registration Page</title>
    <link rel="stylesheet" href="https://cdn.shoelace.style/1.0.0-beta20/shoelace.css">
    <link rel="stylesheet" type="text/css" href="style.css"/>
  </head>
  <body>
    <div class="container">
      <br>
      <h1 class="page-title">Create New Account</h1>
      {{#messages.error}}
          <div class="alert alert-danger" role="alert">
            {{.}}
          </div>
      {{/messages.error}}
      <form class="register" action="/register" method="post">
        <div class="">
          <label for="username">Username: </label>
          <input type="text" name="username" value="" placeholder="Enter Username">
          {{#errors.username}}
            <div class="form-control-feedback alert alert-danger" role="alert">{{msg}}</div>
          {{/errors.username}}
        </div>
        <br>
        <div class="">
          <label for="password">Password: </label>
          <input type="password" name="password" value="" placeholder="Enter Password">
          {{#errors.password}}
            <div class="form-control-feedback alert alert-danger" role="alert">{{msg}}</div>
          {{/errors.password}}
        </div>
        <br>
        <button type="submit" name="button">Add New User</button>
      </form>

    </div>
  </body>
</html>
